{
  "name": "db",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "migrate": "supabase db push",
    "reset": "supabase db reset",
    "generate": "supabase gen types typescript --local > src/types/database.types.ts",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "supabase": "^1.142.0",
    "typescript": "^5.3.3"
  }
}

