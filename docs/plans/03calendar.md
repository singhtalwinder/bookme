Calendar
Views
Business Calendar: multi-staff day/week with column virtualization, filters (staff, seniority, service)
Staff View: self calendar with quick settings (hours, time off, services), personal integrations
Features
Create/edit/reschedule via drag/drop and dialogs
Conflict checks: working hours, time off, existing appts (incl. buffers), external busy, max_concurrent
Quick reassign staff; inline client create/search
Status lifecycle: pending → confirmed → arrived → started → completed → no_show → canceled
Color coding by service/status
Performance: load ≤ 1.5s for 20 visible columns; scalable to 100 staff
Availability Engine
Inputs: working_hours, time_off, appointments (+buffers), external busy, max_concurrent, variants’ durations
Output: slots per staff or Any-staff

External Calendars
Show external busy overlays
2-way sync controls per staff (write/read calendars)
AC
Create and move bookings without conflicts
Status changes persist and drive metrics
Calendar renders responsively and performs under target